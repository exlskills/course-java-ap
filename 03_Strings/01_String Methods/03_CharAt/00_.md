Now that we have explored the concept of index in Java, let’s see the index related functions of other methods. The “charAt” method returns a single character from a specified index. The syntax is as follows, “stringname.charAt(any integer)” and the return is a single character “char” data type, not a “String”. In a case where the index is negative or grater than “stringname.length()-1”, you will receive a runtime error. Let’s see a few examples of the “charAt” method.

